% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/geom_puzzle.R
\name{geom_puzzle_rect}
\alias{geom_puzzle_rect}
\title{Add rectangular puzzle layer to a ggplot}
\usage{
geom_puzzle_rect(
  mapping = NULL,
  data = NULL,
  stat = "puzzle",
  position = "identity",
  rows = 3,
  cols = 3,
  tabsize = 20,
  jitter = 4,
  seed = NULL,
  bezier_resolution = 20,
  ...,
  na.rm = FALSE,
  show.legend = NA,
  inherit.aes = TRUE
)
}
\arguments{
\item{mapping}{Set of aesthetic mappings. Usually includes `fill`.}

\item{data}{The data to visualize. Each row maps to a puzzle piece.}

\item{stat}{The statistical transformation to use (default: "puzzle").}

\item{position}{Position adjustment (default: "identity").}

\item{rows}{Number of rows in the puzzle grid.}

\item{cols}{Number of columns in the puzzle grid.}

\item{tabsize}{Size of the puzzle tabs (default: 20).}

\item{jitter}{Random variation in tab positions (default: 4).}

\item{seed}{Random seed for reproducible puzzle shapes.}

\item{bezier_resolution}{Points per Bezier curve (default: 20).}

\item{...}{Other arguments passed to layer().}

\item{na.rm}{Remove NA values? (default: FALSE).}

\item{show.legend}{Include this layer in the legend? (default: NA).}

\item{inherit.aes}{Inherit aesthetics from the plot? (default: TRUE).}
}
\value{
A ggplot2 layer.
}
\description{
Creates a puzzle visualization where each puzzle piece represents
a data point. The fill aesthetic is typically mapped to a variable
to color-code the pieces.
}
\examples{
\dontrun{
library(ggplot2)

# Basic rectangular puzzle
df <- data.frame(value = 1:9)
ggplot(df, aes(fill = value)) +
  geom_puzzle_rect(rows = 3, cols = 3, seed = 42) +
  scale_fill_viridis_c() +
  theme_void()

# With categorical data
sales <- data.frame(
  region = c("North", "South", "East", "West"),
  value = c(100, 150, 80, 120)
)
ggplot(sales, aes(fill = value)) +
  geom_puzzle_rect(rows = 2, cols = 2, seed = 42) +
  scale_fill_gradient(low = "white", high = "steelblue") +
  theme_void() +
  labs(title = "Sales by Region")
}

}
