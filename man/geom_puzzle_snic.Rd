% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/geom_puzzle.R
\name{geom_puzzle_snic}
\alias{geom_puzzle_snic}
\title{Add SNIC superpixel puzzle layer to a ggplot}
\usage{
geom_puzzle_snic(
  mapping = NULL,
  data = NULL,
  stat = "puzzle",
  position = "identity",
  image_path = NULL,
  n_cells = 50,
  compactness = 0.5,
  width = 100,
  height = 100,
  tabsize = 10,
  jitter = 2,
  min_tab_size = NULL,
  max_tab_size = NULL,
  seed = NULL,
  offset = 0,
  layout = "grid",
  repel_margin = 2,
  repel_max_iter = 100,
  bezier_resolution = 20,
  seed_type = "hexagonal",
  fill_direction = "forward",
  fusion_groups = NULL,
  fusion_style = "none",
  fusion_opacity = 0.3,
  show_labels = FALSE,
  label_color = "black",
  label_size = NULL,
  ...,
  na.rm = FALSE,
  show.legend = NA,
  inherit.aes = TRUE
)
}
\arguments{
\item{mapping}{Set of aesthetic mappings. Usually includes `fill`.}

\item{data}{The data to visualize. Each row maps to a puzzle piece.}

\item{stat}{The statistical transformation to use (default: "puzzle").}

\item{position}{Position adjustment (default: "identity").}

\item{image_path}{Path to the image file for SNIC segmentation.}

\item{n_cells}{Target number of superpixel cells (default: 50).}

\item{compactness}{SNIC compactness parameter (default: 0.5).
Higher values produce more regular shapes.}

\item{width}{Puzzle width in mm (default: 100).}

\item{height}{Puzzle height in mm (default: 100).}

\item{tabsize}{Size of the puzzle tabs (default: 10).}

\item{jitter}{Random variation in tab positions (default: 2).}

\item{min_tab_size}{Minimum tab height in mm (NULL for no constraint).}

\item{max_tab_size}{Maximum tab height in mm (NULL for no constraint).}

\item{seed}{Random seed for reproducible puzzle shapes.}

\item{offset}{Separation between pieces (0 = complete, >0 = separated).}

\item{layout}{Layout algorithm: "grid" (default) or "repel". Repel layout
applies force-directed separation to avoid piece overlap when offset > 0.}

\item{repel_margin}{Minimum gap between pieces for repel layout (default: 2).}

\item{repel_max_iter}{Maximum iterations for repel layout convergence (default: 100).}

\item{bezier_resolution}{Points per Bezier curve (default: 20).}

\item{seed_type}{SNIC seed grid type: "hexagonal", "rectangular", "diamond", "random".}

\item{fill_direction}{Direction for color assignment: "forward" (default) or "reverse".}

\item{fusion_groups}{Piece fusion specification.}

\item{fusion_style}{Style for fused internal edges: "none", "dashed", "solid".}

\item{fusion_opacity}{Opacity for fused edges (0.0 to 1.0).}

\item{show_labels}{Logical; if TRUE, display piece ID labels (default: FALSE).}

\item{label_color}{Color for piece labels (default: "black").}

\item{label_size}{Font size for labels in points. NULL for auto-sizing.}

\item{...}{Other arguments passed to layer().}

\item{na.rm}{Remove NA values? (default: FALSE).}

\item{show.legend}{Include this layer in the legend? (default: NA).}

\item{inherit.aes}{Inherit aesthetics from the plot? (default: TRUE).}
}
\value{
A ggplot2 layer.
}
\description{
Creates a puzzle visualization with image-aware pieces based on
SNIC superpixel segmentation. Each puzzle piece follows natural
boundaries in the source image.
}
\note{
Requires the 'snic' and 'magick' packages to be installed.
  In ggplot2 context, pieces use standard fills (solid/palette),
  not image fills. Use generate_puzzle() for image-filled SVG output.
}
\examples{
\dontrun{
library(ggplot2)

# SNIC puzzle with ~20 superpixels
df <- data.frame(value = 1:20)
ggplot(df, aes(fill = value)) +
  geom_puzzle_snic(image_path = "photo.jpg", n_cells = 20, seed = 42) +
  scale_fill_viridis_c() +
  theme_void()
}

}
