% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/geom_puzzle.R
\name{geom_puzzle_random}
\alias{geom_puzzle_random}
\title{Add random shape puzzle layer to a ggplot}
\usage{
geom_puzzle_random(
  mapping = NULL,
  data = NULL,
  stat = "puzzle",
  position = "identity",
  n_interior = 12,
  n_pieces = NULL,
  width = 100,
  height = 100,
  tabsize = 6,
  jitter = 2,
  min_tab_size = NULL,
  max_tab_size = NULL,
  seed = NULL,
  offset = 0,
  layout = "grid",
  repel_margin = 2,
  repel_max_iter = 100,
  bezier_resolution = 20,
  n_corner = 4,
  fill_direction = "forward",
  fusion_groups = NULL,
  fusion_style = "none",
  fusion_opacity = 0.3,
  show_labels = FALSE,
  label_color = "black",
  label_size = NULL,
  ...,
  na.rm = FALSE,
  show.legend = NA,
  inherit.aes = TRUE
)
}
\arguments{
\item{mapping}{Set of aesthetic mappings. Usually includes `fill`.}

\item{data}{The data to visualize. Each row maps to a puzzle piece.}

\item{stat}{The statistical transformation to use (default: "puzzle").}

\item{position}{Position adjustment (default: "identity").}

\item{n_interior}{Number of interior points that influence piece count.
Actual piece count depends on triangulation.}

\item{n_pieces}{Deprecated alias for n_interior.}

\item{width}{Puzzle width in mm (default: 100).}

\item{height}{Puzzle height in mm (default: 100).}

\item{tabsize}{Tab size as percentage (0-100). Default: 6.}

\item{jitter}{Random variation in tab positions (default: 2).}

\item{min_tab_size}{Minimum tab height in mm (NULL for no constraint).}

\item{max_tab_size}{Maximum tab height in mm (NULL for no constraint).}

\item{seed}{Random seed for reproducible puzzle shapes.}

\item{offset}{Separation offset between pieces (0 = complete puzzle, >0 = separated).}

\item{layout}{Layout algorithm: "grid" (default) or "repel". Repel layout
applies force-directed separation to avoid piece overlap when offset > 0.}

\item{repel_margin}{Minimum gap between pieces for repel layout (default: 2).}

\item{repel_max_iter}{Maximum iterations for repel layout convergence (default: 100).}

\item{bezier_resolution}{Points per Bezier curve (default: 20).}

\item{n_corner}{Number of corners for the base polygon (default: 4 for rectangle).}

\item{fill_direction}{Direction for color assignment: "forward" (default) or "reverse".
Reverses the spatial order of color assignment across pieces.}

\item{fusion_groups}{Piece fusion specification: PILES notation string (e.g., "1-2-3,4-5"),
list of integer vectors, or NULL for no fusion.}

\item{fusion_style}{Style for fused internal edges: "none" (invisible), "dashed", "solid".}

\item{fusion_opacity}{Opacity for fused edges when style != "none" (0.0 to 1.0).}

\item{show_labels}{Logical; if TRUE, display piece ID labels at piece centers (default: FALSE).}

\item{label_color}{Color for piece labels (default: "black").}

\item{label_size}{Font size for labels in points. NULL for auto-sizing based on piece dimensions.}

\item{...}{Other arguments passed to layer().}

\item{na.rm}{Remove NA values? (default: FALSE).}

\item{show.legend}{Include this layer in the legend? (default: NA).}

\item{inherit.aes}{Inherit aesthetics from the plot? (default: TRUE).}
}
\value{
A ggplot2 layer.
}
\description{
Creates a puzzle visualization with randomly-shaped pieces based on
Delaunay triangulation. Each puzzle piece represents a data point.
The pieces have irregular, organic shapes.
}
\note{
When using gradient color scales (e.g., viridis), color boundaries between
  adjacent pieces may still be visible even when fusion is working correctly.
  These are color transitions, not stroke lines. To verify fusion is working,
  test with solid colors using `scale_fill_manual()`.

Requires the 'RCDT' package to be installed.
}
\examples{
\donttest{
library(ggplot2)

# Random shape puzzle with ~12 interior points
df <- data.frame(value = 1:12)
ggplot(df, aes(fill = value)) +
  geom_puzzle_random(n_interior = 12, seed = 42) +
  scale_fill_viridis_c() +
  theme_void()
}

}
