% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/unified_renderer.R
\name{render_puzzle_svg}
\alias{render_puzzle_svg}
\title{Render positioned pieces to SVG}
\usage{
render_puzzle_svg(
  positioned,
  fill = "none",
  fills = NULL,
  stroke_width = 1.5,
  colors = NULL,
  palette = NULL,
  palette_invert = FALSE,
  fill_direction = "forward",
  background = "white",
  opacity = 1,
  show_labels = FALSE,
  label_color = "black",
  label_size = NULL,
  inline = FALSE,
  image_path = NULL
)
}
\arguments{
\item{positioned}{Output from apply_piece_positioning()}

\item{fill}{Fill color for pieces ("none", a color value, or gradient list)}

\item{fills}{Vector of per-piece fill colors (optional, overrides fill parameter).
When provided, each piece gets its own fill color from this vector.}

\item{stroke_width}{Line width for piece strokes}

\item{colors}{Color palette for piece strokes (NULL = use default)}

\item{palette}{Viridis palette name (NULL = use config default)}

\item{palette_invert}{Logical, if TRUE reverses the palette direction (default: FALSE)}

\item{fill_direction}{Character, either "forward" (default) or "reverse".
Controls spatial color assignment order. For ring-based puzzles, reverses
within each ring. For rectangular, reverses entire sequence.}

\item{background}{Background specification:
- "none": No background
- "white", "#FFFFFF", etc.: Solid color background
- list(type="gradient", ...): Gradient background}

\item{opacity}{Piece opacity (0.0 to 1.0)}

\item{show_labels}{Logical; if TRUE, display piece ID labels at piece centers}

\item{label_color}{Color for piece labels (default: "black")}

\item{label_size}{Font size for labels in mm (default: auto-calculated based on piece size)}

\item{inline}{Logical; if TRUE, omit XML declaration for inline HTML embedding.
Use TRUE when embedding SVG in HTML (e.g., Shiny apps). Default: FALSE.}

\item{image_path}{Path to an image file for SNIC image-fill rendering (NULL for no image)}
}
\value{
Complete SVG string
}
\description{
Takes output from apply_piece_positioning() and renders to a complete SVG string.
Supports solid backgrounds, gradient backgrounds, or no background.
}
