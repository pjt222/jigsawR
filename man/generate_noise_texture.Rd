% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/noise_fills.R
\name{generate_noise_texture}
\alias{generate_noise_texture}
\title{Generate noise texture as base64-encoded PNG}
\usage{
generate_noise_texture(
  width = 256,
  height = 256,
  noise_type = "perlin",
  frequency = 0.02,
  octaves = 4,
  lacunarity = 2,
  gain = 0.5,
  seed = NULL,
  color_low = "#1a1a2e",
  color_high = "#eaeaea",
  color_mid = NULL
)
}
\arguments{
\item{width}{Width of noise texture in pixels (default: 256)}

\item{height}{Height of noise texture in pixels (default: 256)}

\item{noise_type}{Type of noise: "perlin", "simplex", "worley", "cubic",
"value", "white" (default: "perlin")}

\item{frequency}{Base frequency of noise (default: 0.02, higher = more detail)}

\item{octaves}{Number of octaves for fractal noise (default: 4)}

\item{lacunarity}{Frequency multiplier per octave (default: 2)}

\item{gain}{Amplitude multiplier per octave (default: 0.5)}

\item{seed}{Random seed for reproducibility (default: NULL = random)}

\item{color_low}{Color for lowest noise values (default: "#1a1a2e")}

\item{color_high}{Color for highest noise values (default: "#eaeaea")}

\item{color_mid}{Optional middle color for 3-stop gradient (default: NULL)}
}
\value{
Base64-encoded PNG string (data URI format)
}
\description{
Creates a procedural noise texture using the ambient package and
encodes it as a base64 PNG string for SVG embedding.
}
\examples{
\dontrun{
# Basic Perlin noise
noise_png <- generate_noise_texture(seed = 42)

# High-frequency Worley noise
noise_png <- generate_noise_texture(
  noise_type = "worley",
  frequency = 0.05,
  color_low = "#000033",
  color_high = "#66ccff"
)
}
}
