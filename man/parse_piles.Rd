% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/piles_notation.R
\name{parse_piles}
\alias{parse_piles}
\title{Parse PILES notation into fusion groups}
\usage{
parse_piles(piles, puzzle_result = NULL, validate = TRUE)
}
\arguments{
\item{piles}{Character string in PILES notation}

\item{puzzle_result}{Optional puzzle result for validation and special keywords}

\item{validate}{If TRUE, validate that all pieces are adjacent (default TRUE)}
}
\value{
List of integer vectors, each representing a fusion group
}
\description{
Converts PILES (Puzzle Input Line Entry System) notation into a list of
fusion groups. PILES is inspired by chemistry's SMILES notation.
}
\details{
PILES Syntax:
\itemize{
  \item Numbers: Piece IDs (e.g., "1", "2", "15")
  \item Hyphen (-): Fusion bond between pieces (e.g., "1-2")
  \item Comma (,) or Period (.): Separate groups (e.g., "1-2,3-4")
  \item Parentheses: Branching (e.g., "1-2(-3)-4" = 2 bonds to 1, 3, 4)
  \item @n: Ring closure (e.g., "1-2-3-4@1" = 4 connects to 1)
  \item n:m: Range (e.g., "1:5" = pieces 1-5)
  \item Special keywords: "center", "ring1", "R1", "C2", etc.
}
}
\examples{
# Simple pair
parse_piles("1-2")

# Linear chain
parse_piles("1-2-3-4")

# Multiple groups
parse_piles("1-2,3-4-5")

# Branched structure (piece 2 connects to 1, 3, and 4)
parse_piles("1-2(-3)-4")

# Ring structure
parse_piles("1-2-3-4@1")

# Range notation
parse_piles("1:6")

}
