% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/tessellation_edge_generation.R
\name{generate_fermat_points}
\alias{generate_fermat_points}
\title{Generate Fermat spiral points}
\usage{
generate_fermat_points(n, size, seed = NULL, center = NULL)
}
\arguments{
\item{n}{Number of points to generate}

\item{size}{Canvas dimensions c(height, width) to match grid c(rows, cols), or c(diameter) for circular}

\item{seed}{Random seed (for small perturbation if desired)}

\item{center}{Optional center point c(x, y), default is center of size}
}
\value{
Data frame with x and y columns
}
\description{
Creates points distributed on a Fermat spiral (also known as golden spiral).
This provides a visually pleasing, relatively uniform distribution that
works well for Voronoi tessellation.
}
\details{
Uses the formula:
- r = c * sqrt(i)
- theta = i * golden_angle

Where golden_angle = pi * (3 - sqrt(5)) radians
}
\examples{
\dontrun{
# Generate 50 points in a 300x300 area
pts <- generate_fermat_points(50, c(300, 300))
plot(pts$x, pts$y, asp = 1)
}
}
